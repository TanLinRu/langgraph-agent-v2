---
description: Run pytest with coverage
agent: build
---
Run the test suite with coverage report:

```bash
pytest --cov=src -v
```

Show me the test results in a clear summary. If there are failures, analyze them and suggest fixes.
